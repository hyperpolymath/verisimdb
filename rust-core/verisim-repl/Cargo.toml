# SPDX-License-Identifier: MPL-2.0

[package]
name = "verisim-repl"
description = "Interactive VCL REPL for VeriSimDB"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
publish = false  # workspace-internal; path deps cannot be published to crates.io

[[bin]]
name = "vcl"
path = "src/main.rs"

[dependencies]
# HTTP client for verisim-api communication
reqwest = { workspace = true, features = ["json", "blocking"] }
rustls.workspace = true
serde.workspace = true
serde_json.workspace = true

# REPL framework and CLI
rustyline = "15"
rustyline-derive = "0.12"
clap = { version = "4", features = ["derive"] }

# Output formatting
comfy-table = "8"
colored = "3"

# Home directory resolution (history file, .vclrc)
dirs = "6"
